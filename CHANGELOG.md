## 4.0.2

- 功能新增：新增 `hashAlgorithm` 参数，支持选择 `xxhash64`（默认）或 `md5` 哈希算法用于数据校验
- 功能优化：多值 INSERT 修复 SQL 在含多行数据时改为每个 value 独占一行，输出更易读
- 性能优化：优化 SQL 解析逻辑，跨多行语句改为整块解析一次，长语句解析开销大幅降低
- 性能优化：引入 XXHash64 算法替代硬编码 MD5，校验性能提升约 15-25 倍
- 问题修复：修复 table 模式下多行 INSERT 修复 SQL 写入 stage 文件时因换行破坏「一行一条 SQL」导致读回错乱的问题
- 测试完善：新增 SQL 压平、跨多行语句往返及 INSERT 拆分相关单元测试
- 测试完善：新增 checksum 模块单元测试，覆盖 XXHash64、MD5、CheckHash 等核心函数
