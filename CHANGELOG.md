## 4.0.1
- [功能优化]: 统一 `resultFile` 参数行为：gt-checksum CSV 报告、repairDB CSV 报告、断点续传进度文件三者共用同一 `resultFile` 参数，repairDB 和断点续传文件自动提取 basedir 并在同目录下生成各自文件，避免文件名冲突。空值、`result`、`.` 三者等价，均使用 `result/` 目录。
- [功能优化]: gc-sample.conf 参数分组重组为 8 个区段（数据源、基本配置、结构校验增强、数据校验增强、核心运行、结果输出、数据修复、DB 兼容性、日志），与手册章节对齐；去除重复参数项。
- [功能优化]: 分区修复逻辑改为集合差分算法，支持非连续分区差异（如删除中部分区、滚动窗口平移等场景），不再局限于尾部追加/删除。
- [功能优化]: 当目标端存在 MAXVALUE 分区且需新增分区时，自动生成 REORGANIZE PARTITION 语句替代 ADD PARTITION，确保新分区插入在 MAXVALUE 之前。
- [功能优化]: 分区比较时自动剥离 ENGINE= 注解（如 `ENGINE=InnoDB` vs `ENGINE=INNODB`），避免跨版本/跨引擎元数据噪音干扰比对结果。
- [功能优化]: TO_DAYS() 类型分区边界整数值自动转换为可读日期（如 `739282` → `2024-02-01`），提升 advisory SQL 可读性。
- [问题修复]: 修复 MySQL→MySQL 同库同表名场景下分区缓存碰撞导致漏报分区差异的问题，改用 db 指针地址区分源/目的连接。
- [测试完善]: 新增分区修复逻辑单元测试，覆盖删除中部分区、MAXVALUE split、滚动窗口平移、带 MAXVALUE 新增等多种场景。
- [测试完善]: 补充 `resultFile` basedir 解析的单元测试，覆盖空值/`result`/`.` 等价性、裸文件名、带目录路径等多种场景。
